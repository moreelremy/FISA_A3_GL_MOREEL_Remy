<Window x:Class="EasySaveGUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EasySaveGUI.ViewModel"
        Title="Settings"
        Height="400"
        Width="500"
        MinHeight="400"
        MinWidth="500"
        MaxHeight="400"
        MaxWidth="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:LanguageHelper x:Key="LangHelper" />
    </Window.Resources>

    <Window.DataContext>
        <local:SettingsWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Rectangle Fill="{StaticResource MaCouleurPrincipale}" Height="42" RadiusX="8" RadiusY="8" Margin="10,10,10,0"/>
            <Label Content="Settings"
                   FontSize="20"
                   FontFamily="Impact"
                   Foreground="White"
                   HorizontalContentAlignment="Center"
                   VerticalContentAlignment="Center"
                   Margin="10,10,10,0"/>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label x:Name="LabelExtension"
                   Content="{Binding Source={StaticResource LangHelper}, Path=[WPF_SettingExtensionToCrypt]}"
                   HorizontalAlignment="Left"
                   Style="{StaticResource CustomLabelStyle}" 
                   Margin="0,10,0,10" 
                   Grid.Row="0"/>

            <TextBox x:Name="InputExtension"
                     Grid.Row="1"
                     Text="{Binding InputSettingsExtension, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                     Margin="0,10,0,10" 
                     Width="200" Height="30"/>

            <Label x:Name="LabelSetting"
                   Content="{Binding Source={StaticResource LangHelper}, Path=[WPF_SettingSoftware]}"
                   HorizontalAlignment="Left"
                   Style="{StaticResource CustomLabelStyle}" 
                   Margin="0,10,0,10" 
                   Grid.Row="2"/>

            <TextBox x:Name="InputSetting"
                     Grid.Row="3"
                     Text="{Binding InputSettingsSoftware, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                     Margin="0,10,0,10" 
                     Width="200" Height="30"/>

            <Button Grid.Row="4"
                    Content="APPLY"
                    Command="{Binding SaveSettingCommand}"
                    Style="{StaticResource CustomButtonStyle}" 
                    HorizontalAlignment="Center" 
                    Margin="0,10,0,10" 
                    Width="122" Height="30"/>
        </Grid>
    </Grid>
</Window>
